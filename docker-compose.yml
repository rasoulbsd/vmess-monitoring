version: '3.8'

services:
  vmess-monitoring:
    build: .
    ports:
      - "${SERVER_PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - ./.env:/app/.env:ro
    restart: unless-stopped 